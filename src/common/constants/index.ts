import { atomKey } from '@constants/atom.constant';
import { router } from '@constants/router.constant';
import { apiServices } from '@constants/api.constant';

export default Object.freeze({
  atomKey,
  router,
  apiServices,
});
